{
  "name": "clarior-monorepo",
  "private": true,
  "workspaces": [
    "frontend1",
    "backend"
  ],
  "scripts": {
    "build": "npm run build --workspace=frontend1",
    "install:frontend": "npm install --workspace=frontend1",
    "start:backend": "npm start --workspace=backend"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
